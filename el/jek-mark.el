(defface visible-mark-face-2
  `((t (:background "blue")))
  "Face for the mark."
  :group 'visible-mark)

(defface visible-mark-face-3
  `((t (:background "purple")))
  "Face for the mark."
  :group 'visible-mark)

(defface visible-mark-face-4
  `((t (:background "purple")))
  "Face for the mark."
  :group 'visible-mark)

(defface visible-mark-face-5
  `((t (:background "purple")))
  "Face for the mark."
  :group 'visible-mark)
